services:
  app:
    build: .
    restart: always
    ports:
      - "3001:3000"
    env_file:
      - .env.production
    volumes:
      - ./data:/app/data
      - ./public/uploads:/app/public/uploads
